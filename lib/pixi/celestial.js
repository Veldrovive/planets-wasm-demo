class Body {
    constructor (container, { centerX, centerY }, { focusX, focusY }, emitter) {
        this.container = container
        this.centerX = centerX
        this.centerY = centerY
        this.focusX = focusX
        this.focusY = focusY
        this.emitter = emitter
    }

    update (ellapsed) {
        
    }
}